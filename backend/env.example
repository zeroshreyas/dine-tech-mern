# =================================
# DINETECH BACKEND CONFIGURATION
# =================================

# Server Configuration
PORT=5000
NODE_ENV=development

# MongoDB Configuration
MONGODB_URI=mongodb://localhost:27017/dine-tech

# JWT Configuration
JWT_SECRET=dinetech-super-secure-jwt-secret-key-change-in-production

# =================================
# EMAIL CONFIGURATION
# =================================

# Option 1: Gmail Configuration (Recommended)
# 1. Go to https://myaccount.google.com
# 2. Security → 2-Step Verification (enable)
# 3. Security → App passwords → Mail
# 4. Copy the 16-digit password below

EMAIL_USER=your-gmail@gmail.com
EMAIL_PASS=abcd-efgh-ijkl-mnop
EMAIL_FROM="DineTech System" <noreply@dinetech.com>

# Option 2: Custom SMTP (Alternative)
# Uncomment these if using custom SMTP instead of Gmail

# SMTP_HOST=smtp.yourdomain.com
# SMTP_PORT=587
# SMTP_SECURE=false
# SMTP_USER=your-email@yourdomain.com
# SMTP_PASS=your-password

# =================================
# APPLICATION CONFIGURATION
# =================================

# Frontend URL (for CORS and email links)
FRONTEND_URL=http://localhost:5173

# Razorpay Configuration (for payments)
RAZORPAY_KEY_ID=your-razorpay-key-id
RAZORPAY_KEY_SECRET=your-razorpay-key-secret 